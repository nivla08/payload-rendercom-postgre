/**
 * Generated by local scaffolder. Safe to edit unless otherwise documented.
 */

import type { GlobalConfig } from 'payload'

import { adminOnly } from '@/cms/access'

export const {{pascalCase name}}: GlobalConfig = {
  slug: '{{kebabCase name}}',
  access: {
    read: () => true,
    update: adminOnly,
  },
  admin: {
    group: 'Globals',
  },
  fields: [],
}
