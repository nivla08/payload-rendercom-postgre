/**
 * Starter default for nested header menus.
 *
 * Increase this if a project needs deeper navigation trees, but keeping the
 * default shallow helps avoid overly complex menu structures in most builds.
 */
export const MAX_HEADER_NAV_DEPTH = 2
